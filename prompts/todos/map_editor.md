Plan on how to extend the current existing HUB map editor to a generalist Map Editor that edits all maps.

For that to happend, include a new top menu for the map selector.

If there is anything in map building/rendering that is not always pre-rendered the same across diffrent playthroughs/campaigns/map loads, make them do not be editable in map editor. Also show a warning message on map editor informing what cannot be edited because is procedurally generating in-game.